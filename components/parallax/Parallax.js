export default function ParallaxLayout(props) {
    return (
        <div>
            {/* <NavigationBar/>
            <main className = {styles.main}>
                {props.children}
            </main> */}
        </div>
    );
}